# 2026-09-08

## Session (four changes)
- Writing feed: `src/lib/medium-loader.ts` fetches Medium RSS at build (fail-soft, tested), merged with local MDX via `src/lib/feed.ts`; drafts hidden in prod. Placeholder drafts deleted. /writing has intro + year groups.
- Domain tags: filled square mark per row, tinted hover via color-mix on `li[data-domain]`, filled chips on /work. Titles stay ink.
- Covers: `src/lib/books-loader.ts` enriches books.md from Open Library (sequential, retry, title-only fallback), saves covers to `public/covers/`, samples dominant colour with jpeg-js, pulls description as blurb (skips Spanish "RESUMEN"). Cache in `.cache/books.json` (gitignored); delete it to refetch. `Cover.astro` = tile with ruled fallback.
- /reading: spines as buttons (thickness=pages, height band=format, lean at index i%4==2 max 2), colour band from cover, native popover panel, JS only toggles `.pulled`. Plain list below.
- Data quality to fix in books.md: Paradise of the Blind pages=52 (wrong OL median), formats guessed as paperback except Austral. Kokoro cover is monochrome so no band.
- Gotcha: stale content store lives in node_modules/.astro; rm it after deleting content files.
- Latest preview: https://personal-site-4iwaeyso6-trinav-codes-projects.vercel.app
- copy.ts at root: every user-facing string, by page/section; components import `copy`. Count-dependent strings are functions.
- Shelf moved to home Currently (Shelf.astro, current + 10 recent, scale .85, vertical spine titles); /reading deleted, "The shelf" link removed, reading copy folded into copy.home.currently. Panel shows cover/title/author/date/thoughts (blurb still fetched, not rendered).
- books.md rewritten with 17 books (2 current, 15 recent; football six as `finished: 2025`, order unknown). Schema: tags, source. 15/17 covers; Brief History of Intelligence + Expected Goals have no cover on OL → set `cover:` URL manually.
- Loader: author-checked cover pick, year-only finished coerced, Goodreads "read" shelf RSS merge (feeds.goodreads in site.config.ts, empty = skipped; file wins on title match).
- Dev-only editor: `integrations/books-editor.ts` injects /edit-books + /__books JSON API (dev only); `src/dev/EditBooks.astro` drag-reorder, inline fields, add/remove, Save writes books.md (roundtrip verified). Astro 7 `astro dev` is a daemon: `npx astro dev status|stop`.
