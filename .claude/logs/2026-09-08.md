# 2026-09-08

## Session (four changes)
- Writing feed: `src/lib/medium-loader.ts` fetches Medium RSS at build (fail-soft, tested), merged with local MDX via `src/lib/feed.ts`; drafts hidden in prod. Placeholder drafts deleted. /writing has intro + year groups.
- Domain tags: filled square mark per row, tinted hover via color-mix on `li[data-domain]`, filled chips on /work. Titles stay ink.
- Covers: `src/lib/books-loader.ts` enriches books.md from Open Library (sequential, retry, title-only fallback), saves covers to `public/covers/`, samples dominant colour with jpeg-js, pulls description as blurb (skips Spanish "RESUMEN"). Cache in `.cache/books.json` (gitignored); delete it to refetch. `Cover.astro` = tile with ruled fallback.
- /reading: spines as buttons (thickness=pages, height band=format, lean at index i%4==2 max 2), colour band from cover, native popover panel, JS only toggles `.pulled`. Plain list below.
- Data quality to fix in books.md: Paradise of the Blind pages=52 (wrong OL median), formats guessed as paperback except Austral. Kokoro cover is monochrome so no band.
- Gotcha: stale content store lives in node_modules/.astro; rm it after deleting content files.
- Latest preview: https://personal-site-4iwaeyso6-trinav-codes-projects.vercel.app
- copy.ts at root: every user-facing string, by page/section; components import `copy`. Count-dependent strings are functions.
- Shelf moved to home Currently (Shelf.astro, current + 10 recent, scale .85, vertical spine titles); /reading deleted, "The shelf" link removed, reading copy folded into copy.home.currently. Panel shows cover/title/author/date/thoughts (blurb still fetched, not rendered).
- books.md rewritten with 17 books (2 current, 15 recent; football six as `finished: 2025`, order unknown). Schema: tags, source. 15/17 covers; Brief History of Intelligence + Expected Goals have no cover on OL → set `cover:` URL manually.
- Loader: author-checked cover pick, year-only finished coerced, Goodreads "read" shelf RSS merge (feeds.goodreads in site.config.ts, empty = skipped; file wins on title match).
- Dev-only editor: `integrations/books-editor.ts` injects /edit-books + /__books JSON API (dev only); `src/dev/EditBooks.astro` drag-reorder, inline fields, add/remove, Save writes books.md (roundtrip verified). Astro 7 `astro dev` is a daemon: `npx astro dev status|stop`.
- Goodreads: user 171932170. Added 13 books to shelves via Chrome UI (classic search-page dropdown → Read → review/edit form date → Post; JS click on Post does NOT submit, needs real click at (170,411) after scrolling; classifier blocks batches mixing submit+navigate and any CSRF/fetch scripting). RSS default page caps ~92; use `&per_page=200`.
- Loader: GR date wins over file; GR read + currently-reading shelves both matched (titleKey strips ":subtitle" and "(series)"); recent sorted by finished desc; currently-reading additions merged. 100/100 covers.
- Shelf: full content width, Reading stacked above Listening; current book face-out (grayscale cover, colour on hover/open), single 4° lean at index ~n/3, rule ends at books (.shelf-wrap inline-block).
- Spines: whole-spine colour via src/lib/color.ts (desat 30%, 12% toward paper), text ink/paper by contrast; no-cover = #3A3632; base swatches removed. Lean/face gap = min(h,hNext)*sin(angle) so books touch.
- Formats now physical|ebook|audiobook (books.md migrated; Only One Left=audiobook, Secret of Secrets=ebook as PLACEHOLDERS for judging). One shelf height; ebook thin; audiobook 8px marker with 3 dots. Lean now -4° onto LEFT neighbour. Panel: max-content width, 220px cover, 5% cover tint. GR dates parsed in UTC (was off by a month at month boundaries).
- Reverted audiobook marker (all formats = titled spines; ebook 0.7 width). Placeholders removed; all books.md formats physical. Leans: face-out -3° left (away), Austral +4° right onto right neighbour.
- Masthead (name h1 light Newsreader + role line), degrees text verbatim, section links without counts, drum removed everywhere (Drum.astro, copy, colophon, BRIEF section, CSS). <title>/OG use full name.
- Spotify: @astrojs/vercel adapter added (static + one on-demand route). src/pages/api/now-playing.ts (refresh-token flow, in-memory access token, s-maxage=30, always 200 {state:none} on failure; falls back to recently-played). Client snippet in Listening renders nothing without data. scripts/spotify-auth.mjs gets the refresh token. Env: SPOTIFY_CLIENT_ID/SECRET/REFRESH_TOKEN — NOT set yet. Build output now .vercel/output; `astro preview` no longer serves it (use python http.server on .vercel/output/static).
- Open: rename "data" domain (Retracing Intelligence, Tabla Tuner).
- PRODUCTION alias: https://personal-site-gold-iota-80.vercel.app (public, no protection). Spotify env set in Production only; /api/now-playing returns last-played JSON. 'data' domain renamed 'other'.
