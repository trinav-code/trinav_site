---
import { copy } from '../../copy';
import Base from '../layouts/Base.astro';
---
<Base title={copy.notFound.title}>
  <header class="page-head">
    <h1>{copy.notFound.heading}</h1>
    <p><a href="/">{copy.notFound.back}</a></p>
  </header>
</Base>
