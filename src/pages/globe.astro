---
import { copy } from '../../copy';
import Base from '../layouts/Base.astro';
---
<Base title={copy.globe.title} description={copy.globe.description}>
  <header class="page-head">
    <h1>{copy.globe.heading}</h1>
    <p>{copy.globe.intro}</p>
  </header>
</Base>
